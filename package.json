{
  "name": "health-data-bridge",
  "private": true,
  "version": "1.0.5",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build && node scripts/finalize-web-build.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "test": "playwright test",
    "cap:sync": "npm run build && node scripts/prepare-native-dist.mjs && HEALTH_DATA_BRIDGE_CAP_WEB_DIR=dist-native cap sync android && node scripts/exclude-android-download.mjs",
    "android:build": "npm run cap:sync && cd android && ./gradlew :app:clean :app:assembleDebug test assembleDebugAndroidTest",
    "android:device-test": "cd android && ./gradlew --no-daemon --max-workers=1 :app:connectedDebugAndroidTest",
    "android:verify": "node scripts/verify-android-apk.mjs",
    "android:publish": "npm run android:build && npm run android:verify -- --native && node scripts/publish-android-apk.mjs && npm run android:verify -- --published && npm run build"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@capacitor/cli": "^7.4.3",
    "@playwright/test": "1.58.2",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
