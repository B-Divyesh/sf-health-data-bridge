[
  {
    "id": "duplicate-safe",
    "claim": "A second import writes zero duplicate records.",
    "where": "landing preview, bridge receipt, README",
    "test": "npm test -- --grep @claim:duplicate-safe",
    "sandbox": "fresh browser context at /demo; import the 12 sample records twice"
  },
  {
    "id": "batch-duplicate-safe",
    "claim": "Repeated IDs in one import are written once.",
    "where": "bridge receipt",
    "test": "npm test -- --grep @claim:batch-duplicate-safe",
    "sandbox": "fresh browser context at /bridge; open a two-record JSON export with the same ID and inspect its receipt and JSON export"
  },
  {
    "id": "date-range-map",
    "claim": "Choose dates to preview only matching records.",
    "where": "landing route section, bridge range station, README",
    "test": "npm test -- --grep @claim:date-range-map",
    "sandbox": "fresh browser contexts at /demo and /bridge; verify the normal and reversed ranges, then verify local-midnight records in America/Los_Angeles and Asia/Kolkata"
  },
  {
    "id": "csv-export",
    "claim": "Exports the local log as CSV.",
    "where": "bridge export toolbar, README",
    "test": "npm test -- --grep @claim:csv-export",
    "sandbox": "fresh browser context at /demo; import sample records and inspect the download"
  },
  {
    "id": "json-export",
    "claim": "Exports records and receipts as JSON.",
    "where": "bridge export toolbar, README",
    "test": "npm test -- --grep @claim:json-export",
    "sandbox": "fresh browser context at /demo; import sample records and inspect the download"
  },
  {
    "id": "local-only",
    "claim": "Health records stay on the device.",
    "where": "landing hero, privacy page, README",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "fresh browser context at /demo; monitor the complete import and export flow for off-origin requests"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing hero, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context at /demo; install the service worker, go offline, reload, and preview records"
  },
  {
    "id": "encrypted-storage",
    "claim": "Local records and receipts use device-bound encryption.",
    "where": "privacy page, README",
    "test": "npm test -- --grep @claim:encrypted-storage",
    "sandbox": "fresh browser context at /bridge; import a unique record and inspect IndexedDB for plaintext"
  },
  {
    "id": "paid-custom-fields",
    "claim": "Bridge Plus saves custom export field names.",
    "where": "landing paid section, bridge mapping station, README",
    "test": "npm test -- --grep @claim:paid-custom-fields",
    "sandbox": "fresh browser context with a cached valid test verdict; change a field name and preview its mapping"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo sample data and preferences are never saved to real data.",
    "where": "demo banner, demo documentation",
    "test": "npm test -- --grep @claim:demo-isolation",
    "sandbox": "fresh browser context seeded with a real custom field; change and reset demo field names, then start for real"
  },
  {
    "id": "four-station-flow",
    "claim": "The import route has four stations: source, range, map, and receipt.",
    "where": "landing route section, demo progress rail",
    "test": "npm test -- --grep @claim:four-station-flow",
    "sandbox": "fresh browser context at /demo; inspect the accessible progress list"
  },
  {
    "id": "local-file-import",
    "claim": "The web build opens local Health Connect JSON and CSV exports.",
    "where": "bridge source station, README",
    "test": "npm test -- --grep @claim:local-file-import",
    "sandbox": "fresh browser context at /bridge; open one JSON fixture and one CSV fixture"
  },
  {
    "id": "narrow-health-permissions",
    "claim": "The Android bridge requests only steps, active calories, exercise, and weight reads.",
    "where": "bridge source station, README",
    "test": "npm test -- --grep @claim:narrow-health-permissions",
    "sandbox": "repository Android manifest; assert the exact health permission list and no write scope"
  },
  {
    "id": "android-native-package",
    "claim": "The Android download contains the registered native Health Connect bridge.",
    "where": "landing Android download link, README",
    "test": "npm test -- --grep @claim:android-native-package",
    "sandbox": "checked-in debug APK and Android activity source; confirm package, native registrar, and four read-only record methods"
  },
  {
    "id": "free-core-flow",
    "claim": "Core import and export are free.",
    "where": "landing facts, paid section, README",
    "test": "npm test -- --grep @claim:free-core-flow",
    "sandbox": "fresh browser context with no license; import the sample and export CSV and JSON"
  },
  {
    "id": "no-account-cloud-history",
    "claim": "There is no account or cloud history.",
    "where": "landing boundaries",
    "test": "npm test -- --grep @claim:no-account-cloud-history",
    "sandbox": "fresh browser context; run the complete demo flow and assert there is no account control or off-origin request"
  },
  {
    "id": "no-provider-sharing",
    "claim": "The bridge does not share records with a provider.",
    "where": "landing boundaries, README",
    "test": "npm test -- --grep @claim:no-provider-sharing",
    "sandbox": "fresh browser context; import and export sample records while recording every request"
  },
  {
    "id": "sales-paused",
    "claim": "New Bridge Plus purchases are paused.",
    "where": "landing paid section, terms, README",
    "test": "npm test -- --grep @claim:sales-paused",
    "sandbox": "fresh browser context; assert the paused notice is present and no checkout action is offered"
  },
  {
    "id": "apk-checksum",
    "claim": "The displayed SHA-256 identifies the downloadable Android test build.",
    "where": "landing Android download",
    "test": "npm test -- --grep @claim:apk-checksum",
    "sandbox": "fresh browser context; download the APK and compare its SHA-256 to the displayed digest"
  },
  {
    "id": "no-medical-calculator",
    "claim": "The bridge does not give medical advice or calculate calorie targets.",
    "where": "landing boundaries, terms, README",
    "test": "npm test -- --grep @claim:no-medical-calculator",
    "sandbox": "fresh browser context; inspect the supported source fields and confirm no advice or target control is offered"
  },
  {
    "id": "no-apple-health-import",
    "claim": "This version does not import Apple Health.",
    "where": "landing boundaries, README",
    "test": "npm test -- --grep @claim:no-apple-health-import",
    "sandbox": "fresh browser context and Android source; confirm the app exposes only Health Connect and local JSON/CSV inputs"
  }
]
